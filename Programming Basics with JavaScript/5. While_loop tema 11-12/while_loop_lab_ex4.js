function numbers(input) {
    let numberBoundary = input[0];
    let number = 1;
    while (number <= numberBoundary) {
        console.log(number)
        number = 2 * number + 1;
        
    }



}

// numbers(["3"])
// numbers(["8"])
// numbers(["17"])
numbers(["31"])